apiVersion: apps/v1
kind: Deployment
metadata:
  name: ravi-springboot
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ravi-springboot
  template:
    metadata:
      labels:
        app: ravi-springboot
    spec:
      containers:
        - name: springboot
          image: ravichalla/docker-demo:docker-demo
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: ravi-springboot
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: ravi-springboot
